import { useState } from "react";

export const Tabs = ({list,activeTab,onTabSwitch})=> {
    return (
        <div className="sticy z-1900 bg-white">
            <div className="container mx-auto flex alig-center py-2 border-b-gray-400 border-b-1">
                {list.map((item,index)=>{
                    return()
                })}
            </div>
        </div>
    )
}